export enum ORDER_STATUS_ENUM {
  orderPlaced = "ORDER_PLACED",
  modified = "MODIFIED",
  canceled = "CANCELED",
  partiallyCanceled = "PARTIALLY_CANCELED",
}

export enum ACCOUNT_WISE_ORDER_STATUS_ENUM {
  completed = "COMPLETED",
  error = "ERROR",
}
